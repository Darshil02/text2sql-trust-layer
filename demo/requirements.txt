# Minimal dependencies for the Streamlit Cloud demo (demo/app.py).
# The demo only reads demo_cache.json and renders — no langchain / chromadb /
# sentence-transformers / duckdb / torch needed.
streamlit==1.58.0
pandas==3.0.3
sqlglot==30.9.0
